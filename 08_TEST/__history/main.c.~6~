#include "main.h"

// UART???: 9???(PIN_E2)?TX???????????9600bps
#use rs232(baud=9600, parity=N, xmit=PIN_G0, bits=8, stream=PC, FORCE_SW, ERRORS)

// ???????????????????
#define LED1   PIN_A1
#define LED2   PIN_A5

void main()
{
    // ????????????????????????????I/O???????
    setup_adc_ports(NO_ANALOGS);

    // ??????
    while(TRUE)
    {
        // 29????30????ON (HIGH)
        output_high(LED0);
        output_high(LED1);

        // ???UART???
        fprintf(PC, "LED ON\r\n");

        // 1???
        delay_ms(1000);

        // 29????30????OFF (LOW)
        output_low(LED0);
        output_low(LED1);

        // ???UART???
        fprintf(PC, "LED OFF\r\n");

        // 1???
        delay_ms(1000);
    }
}
